# Messaging

The through-line: we are building developer tools in order to serve the people using them well.
That is the point of the org — not the size of the catalogue. Keep the counts as supporting
detail, never as the headline.

## One-line description
OutOf0 builds developer tools to serve developers well.

## Short bio
We're building developer tools to serve you well — local-first, MIT licensed.

## Longer bio
OutOf0 is an independent studio building developer tools that respect the people using them.
Everything is local-first and MIT licensed: the input stays on your machine, and the tool stays
out of your way.

## Where this copy is used

| Line | Lives in |
| --- | --- |
| Short bio | `README.md` hero, GitHub organization profile |
| Longer bio | `src/pages/index.astro` — hero intro, `meta description`, `og:description` |
| Tagline | `logos/` lockups ("From zero, forward.") |

Change the copy here first, then mirror it. `README.md` and `src/pages/index.astro` are the only
two places that carry it.
